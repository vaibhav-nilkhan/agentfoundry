{
  "name": "@agentfoundry/json-validator",
  "version": "1.0.0",
  "description": "Automatic JSON schema validation with auto-fix, retry logic, and schema generation for reliable LLM outputs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src tests --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "json",
    "validation",
    "schema",
    "auto-fix",
    "llm",
    "reliability"
  ],
  "author": "AgentFoundry Team",
  "license": "MIT",
  "dependencies": {
    "zod": "^3.22.0",
    "ajv": "^8.12.0",
    "jsonrepair": "^3.6.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
